package model;

/**
The TypeStage enum represents the different types of stages that a project can have.
It contains the following values: START, ANALYSIS, EJECUTION, CLOSURE, MONITOR, and CONTROL.
*/
public enum TypeStage{

    /**
     * Possible stages type.
     */
    START, ANALYSIS, EJECUTION, CLOSURE, MONITOR, CONTROL

}